export type UserWallet = {
  emoji?: string;
  nickname: string;
  address: string;
  createdAt: string;
  notifications: boolean;
};

export type UserWatchlist = {
  id: string;
  ca: string;
  createdAt: string;
  updatedAt: string;
};
